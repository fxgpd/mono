import { Body1Directive } from './body-1.directive';

describe('Body1Directive', () => {
  it('should create an instance', () => {
    const directive = new Body1Directive();
    expect(directive).toBeTruthy();
  });
});
