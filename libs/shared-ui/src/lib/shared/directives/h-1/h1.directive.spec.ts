import { H1Directive } from './h1.directive';

describe('H1Directive', () => {
  it('should create an instance', () => {
    const directive = new H1Directive();
    expect(directive).toBeTruthy();
  });
});
